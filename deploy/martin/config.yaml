listen_addresses: "0.0.0.0:3000"

route_prefix: /martin

postgres:
  connection_string: "${DATABASE_URL}"
  auto_publish: false  # disable auto-publish — only expose explicitly listed sources

  functions:
    boundary_stats:
      schema: public
      function: georiva_boundary_stats